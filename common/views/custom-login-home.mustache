<!DOCTYPE html>
<html lang="en">
  <head>
    {{>head}}
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body id="samples">

    {{>menu}}

    <div id="content" class="container py-3">
      <div id="doc" class="col-md-8 mx-auto">
        <div class="doc-overview text container">

          <h2 class="divider" data-se="overview-doc-header">
            Custom Hosted Login + Express JS Example
          </h2>

          {{^isLoggedIn}}
            <p>Hello!</p>
            <p>If you're viewing this page then you have successfully configured this example server.  Your server configuration is listed below.</p>
            <!-- some other code -->
          {{/isLoggedIn}}

          {{#isLoggedIn}}
            <!-- some other code -->
          {{/isLoggedIn}}

          {{^isLoggedIn}}
            <form method="get" action="/login">
              <button id="login-button" class="btn btn-primary" type="submit">Log In</button>
            </form>
          {{/isLoggedIn}}
        </div>
      </div>

      {{>server-config}}

    </div>

    <!-- Bootstrap JS and jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
  </body>
</html>
